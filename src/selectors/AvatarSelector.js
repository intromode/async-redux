export const getAvatarsSelector = state => state.avatars.list;

//is avatars avatars here because of what I name it in the reducer's index.js? because the state has all the reducers in it which has its state which has the list??


